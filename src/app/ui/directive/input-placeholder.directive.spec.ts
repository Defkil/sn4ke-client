import { InputPlaceholderDirective } from './input-placeholder.directive';

describe('InputPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
